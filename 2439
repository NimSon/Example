#include <iostream>

int main() {

	int N;
	std::cin >> N;

	for (int i = 0; i < N; ++i) {
		for (int j = N - i - 1; j > 0; --j) {
			std::cout << " ";
		}
		for (int k = 0; k <= i; ++k) {
			std::cout << "*";
		}
		std::cout << "\n";
	}

	return 0;
}
