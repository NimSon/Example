#include <iostream>

int main(void) {

	int N;
	std::cin >> N;

	for (int i = 1; i <= (2 * N) - 1; ++i) {
		for (int j = N; j > abs(N - i); --j) {
			std::cout << "*";
		}
		std::cout << "\n";
	}

	return 0;
}
