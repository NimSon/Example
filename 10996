#include <iostream>

int main() {

	int N;
	std::cin >> N;

	for (int i = 1; i <= 2 * N; ++i) {
		for (int j = 1; j <= N; ++j) {
			std::cout << ((i + j) % 2 ? " " : "*");
		}
		std::cout << '\n';
	}

	return 0;
}
