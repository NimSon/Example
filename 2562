#include <iostream>

using namespace std;

int main(void) {

	int num, max = 0;
	int count = 0;
	for (int i = 1; i <= 9; ++i) {
		cin >> num;
		if (num > max) {
			max = num;
			count = i;
		}
	}

	cout << max << '\n' << count;

	return 0;
}
